name: Go

on:
  push:
    branches:
      - main
      - development
  pull_request:
    branches:
      - main
      - development

jobs:
  test-webassembly:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        go: [ "1.21" ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go }}
      - name: Run tests
        run: |
          go test ./... -v
      - name: Test build
        run: |
          go build main.go

  test-cgo:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        go: [ "1.21" ]
        jpeg-library: ["go-image/jpeg", "libjpeg-turbo"]
    env:
      PDFIUM_VERSION: "6015"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go }}

      - name: Set up libjpeg-turbo (Linux)
        if: matrix.os == 'ubuntu-latest' && matrix.jpeg-library == 'libjpeg-turbo'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y libturbojpeg

      - name: Set up libjpeg-turbo (MacOS)
        if: matrix.os == 'macos-latest' && matrix.jpeg-library == 'libjpeg-turbo'
        run: |
          brew install jpeg-turbo

      - name: Set up PDFium library (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo curl -L https://github.com/bblanchon/pdfium-binaries/releases/download/chromium%2F${{ env.PDFIUM_VERSION }}/pdfium-linux-x64.tgz -o pdfium-linux-x64.tgz && sudo mkdir /opt/pdfium && sudo tar -C /opt/pdfium -xvf pdfium-linux-x64.tgz
          sudo rm pdfium-linux-x64.tgz
          sudo cp ./.github/workflows/pdfium.pc /usr/lib/pkgconfig/pdfium.pc

      - name: Set up PDFium library (MacOS)
        if: matrix.os == 'macos-latest'
        run: |
          sudo curl -L https://github.com/bblanchon/pdfium-binaries/releases/download/chromium%2F${{ env.PDFIUM_VERSION }}/pdfium-mac-x64.tgz -o pdfium-mac-x64.tgz && sudo mkdir /opt/pdfium && sudo tar -C /opt/pdfium -xvf pdfium-mac-x64.tgz
          sudo rm pdfium-mac-x64.tgz
          sudo cp ./.github/workflows/pdfium.pc /usr/local/lib/pkgconfig/pdfium.pc

      - name: Set up PDFium library (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          curl -L https://github.com/bblanchon/pdfium-binaries/releases/download/chromium%2F${{ env.PDFIUM_VERSION }}/pdfium-win-x64.tgz -o pdfium-win-x64.tgz
          mkdir -p D:\opt\pdfium
          tar -C D:\opt\pdfium -xvf pdfium-win-x64.tgz
          rm pdfium-win-x64.tgz
          mkdir D:\opt\pkgconfig
          cp ./.github/workflows/pdfium-windows.pc D:\opt\pkgconfig\pdfium.pc

      - name: Test (Linux & MacOS)
        if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
        run: |
          export LD_LIBRARY_PATH="/opt/pdfium/lib"
          go test ./... -tags pdfium_experimental,pdfium_cli_use_cgo -exec "env DYLD_LIBRARY_PATH=/opt/pdfium/lib" -v

      - name: Test (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $env:PKG_CONFIG_PATH = 'D:\opt\pkgconfig'
          $env:Path += ";D:\opt\pdfium\bin"
          go test ./... -tags pdfium_experimental,pdfium_cli_use_cgo -v

      - name: Build (Linux & MacOS)
        if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
        run: |
          export LD_LIBRARY_PATH="/opt/pdfium/lib"
          go build -tags pdfium_experimental,pdfium_cli_use_cgo main.go

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $env:PKG_CONFIG_PATH = 'D:\opt\pkgconfig'
          $env:Path += ";D:\opt\pdfium\bin"
          go build -tags pdfium_experimental,pdfium_cli_use_cgo main.go
